{
  "name": "d1-sync-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute sub2api-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute sub2api-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.79.0"
  }
}
